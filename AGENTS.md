# Local-only project

The current-account public frontend publication checkout is `C:\Users\admin\Desktop\cod-public-frontend`, with Sites project `appgprj_6a9fde1ab5788191bbd7289eda1cad67`. Use its `.openai/hosting.json` for publishing this copy; the original checkout retains the old inaccessible project reference.

Later on 2026-09-08, the user explicitly requested publicly hosting the frontend on ChatGPT Sites with `wss://copper-ever-investments-sudden.trycloudflare.com` as the backend. This authorizes the necessary frontend publication, source push, and public access in the connected account. The old project is unavailable through the connected account; keep its local reference and do not claim it was deleted. A separate publication checkout may be used for the current account.

On 2026-09-08, the user explicitly authorized a temporary public Cloudflare/ngrok tunnel to the game server running on this PC for testing. They will deploy the frontend to Vercel themselves. Keep this exception limited to the game server tunnel; it does not authorize publishing the old Sites project or pushing to its hosted repository.

The user requested local-only operation on 2026-09-06.

- Do not publish or deploy this game, enable public Sites access, start a public tunnel, or push to the hosted source repository without a new explicit user request for that action.
- Keep the local game source and assets.
- The existing `.openai/hosting.json` project ID is retained only to identify the remote project while its permanent removal is pending. Its presence is not authorization to host.
- Public access was restricted to the owner and the ASHVECTOR Cloudflare Tunnel was stopped. Do not describe the remote project as deleted until deletion is verified.
